tosca_definitions_version: cloudify_dsl_1_5

imports:
  - cloudify/types/types.yaml

data_types:

  bar:
    properties:
      baz:
        type: string
      qux:
        type: string

inputs:

  taco:
    type: list
    item_type: bar

